#This is a very simple countdown to liftoff for a space shuttle:
countdown = 10
print("Starting the countdown to liftoff!")
while countdown >= 0:
  #Print the current value of count.
  print(countdown)
  #Incremental counting
  countdown -= 1
print("We have liftoff!")
